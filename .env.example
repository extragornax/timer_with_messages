# Copy to .env and set a strong secret.
# Required for the /admin page to authorize message deletion.
ADMIN_TOKEN=change-me

# Optional: start date for the timer (RFC 3339). Defaults if unset.
TARGET_DATE=2026-07-25T12:00:00Z
